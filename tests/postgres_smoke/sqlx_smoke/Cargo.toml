[package]
name = "pg-gateway-postgres-sqlx-smoke"
version = "0.1.0"
edition = "2024"

[workspace]

[dependencies]
sqlx = { version = "0.8", default-features = false, features = ["runtime-tokio", "postgres"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread", "time"] }
